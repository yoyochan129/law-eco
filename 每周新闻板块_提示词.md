# 任务：在「法律和金融研究动向」网站新增「每周新闻」板块

## 背景与目标

现有网站（`docs/index.html`）展示的是学术研究文章（working paper、期刊论文、博客文章）。
现在需要新增一个**独立的"每周新闻"板块**，抓取央行/监管机构的官方新闻稿、新闻发布和演讲，
与现有的"研究文章"板块并列展示，但内容形态不同（新闻/公告/讲话 ≠ 学术论文），因此建议
作为**独立的数据集合与独立的页面板块**处理，而不是混进现有的 `articles.json` / 文章列表里。

---

## 一、数据来源清单（共6个，均已实测确认可抓取）

| 来源 | 抓取方式 | 已验证可用地址 |
|---|---|---|
| BIS speech | RSS | `https://www.bis.org/doclist/mgmtspeeches.rss`（**注意**：这个来源当前已被现有系统作为"研究文章"抓取，本次改造需要把它从研究文章板块**迁移**到新闻板块，避免同一来源在两处重复出现） |
| SEC Press Releases | RSS | `https://www.sec.gov/news/pressreleases.rss` |
| SEC Speeches & Statements | RSS | `https://www.sec.gov/news/speeches-statements.rss` |
| ECB News & Publications（Press release） | RSS | `https://www.ecb.europa.eu/rss/press.html`（**注意**：此feed混合了新闻稿、演讲、货币政策决议等多种类型，不是纯"Press release"，需要在抓取后按标题/类型做二次过滤，或抓取时保留类型标签供前端筛选，不要假设feed已经按用户给的URL参数`name_of_publication=Press%20release`过滤好） |
| Federal Reserve News & Events | RSS | 官网 `newsevents.htm` 聚合了多种类型，建议同时抓取两个官方分类feed并合并：`https://www.federalreserve.gov/feeds/press_all.xml`（新闻稿全部类别）+ `https://www.federalreserve.gov/feeds/speeches_and_testimony.xml`（演讲与证词） |
| Bank of England News | RSS | `https://www.bankofengland.co.uk/rss/news` |

以上地址均已用 `curl` 实测返回 200 且为有效 RSS/XML，可直接用 `feedparser` 解析，**不需要**
无头浏览器或反爬绕过（这一点与本项目其余研究类来源不同，新闻类官网普遍对RSS抓取更友好）。

---

## 二、抓取字段与已知数据缺口

每条新闻需要产出以下字段：
- **标题**（完整标题）
- **作者或发言人**
- **日期**
- **对内容的中文概括**（2-3句话，**是提炼性概括，不是逐字翻译**——这一点与现有研究文章板块的
  "标题/摘要中文翻译"功能不同，新闻概括应该是"这条新闻讲了什么、要点是什么"的简短总结）
- **原文链接**
- 来源标签（BIS / SEC / ECB / Fed / BoE，用于前端筛选）

### 各来源在"作者/发言人"字段上的已知情况(需要针对性处理)

- **SEC Press Releases**：RSS本身没有发言人字段，机构类新闻稿本身也未必有个人发言人，作者字段可留空或填"U.S. Securities and Exchange Commission"
- **SEC Speeches & Statements**：RSS的summary字段通常是空的，需要额外抓取详情页正文，
  从页面开头的署名行（一般格式为"by [Name], [Title]"）提取发言人姓名；标题本身有时也会
  包含"Statement by Commissioner X"这类线索
- **ECB**：这个feed里的演讲类条目标题本身自带发言人前缀，格式通常是
  `"[姓名]: [标题]"`（例如 `"Philip R. Lane: Outlook for the euro area economy"`），
  可以直接用标题里冒号前的部分作为发言人；纯新闻稿/决议类条目没有这个前缀，发言人留空
- **Fed**：新闻稿(press_all.xml)一般没有个人发言人（机构行为，如"Federal Reserve Board
  issues enforcement action..."），演讲类feed(speeches_and_testimony.xml)的标题或详情页
  通常会包含理事姓名
- **BoE**：新闻稿类内容多为机构行为（PRA/FPC相关），一般没有个人发言人，留空即可；
  如果标题里出现"Letter from the Governor..."这类可以直接识别出"Governor"作为发言人
- **BIS speech**：迁移沿用现有系统里 `parse_rss_generic` 已经在用的 `feedparser` `author`
  字段解析逻辑即可，此前已验证可以正确取到发言人姓名

### 中文概括的生成方式

新闻类"2-3句中文概括"属于**内容提炼**，不是逐句翻译，优先由具备语言理解能力的模型
（例如运行本流水线的 agent 本身，或调用一次 LLM API）在抓取完成后基于新闻原文生成。

如果最终是通过 GitHub Actions 全自动无人值守运行、且不接入任何 LLM API，退化规则如下：
- **如果新闻原文本身自带摘要**（例如 RSS 的 `description`/`summary` 字段就是一段完整摘要，
  或详情页有独立的摘要区块，而不是正文节选），**把这段摘要整段机器翻译成中文**（用现有
  `deep-translator` 逻辑即可），不要只翻译前2-3句、不要截断
- **如果原文没有摘要**（比如 SEC speeches 的 RSS summary 是空的，详情页也只有正文没有
  独立摘要），**这条新闻的中文概括字段就留空，不要用正文前几句拼凑一个"伪摘要"**，
  避免给读者传达"这就是概括"的错误印象；前端在概括字段为空时应显示"（无摘要，详见原文）"
  之类的占位文案，而不是隐藏这条新闻

---

## 三、抓取与更新逻辑

沿用现有项目 `scripts/scrape.py` 的整体模式：
1. 新增 `scripts/scrape_news.py`（或在现有 `sources_config.py` 里新增一组 `type: "news_rss"`
   的来源配置，复用现有 `parse_rss_generic` 逻辑，仅目标数据结构和输出文件不同）
2. 按 URL 去重，只处理"上次抓取之后新出现"的新闻条目（与现有研究文章增量逻辑一致）
3. 输出到独立文件 `database/news.json`，字段结构：
```json
{
  "id": "",
  "title": "",
  "speaker": "",
  "date": "",
  "summary_zh": "",
  "url": "",
  "source": "",
  "date_added": ""
}
```
4. 与现有周报生成脚本 `build_report.py` 集成：在周报 Markdown 里新增一个
   `## 本周新闻速览` 章节，按来源分组列出（标题、发言人、日期、中文概括、链接）
5. 迁移 BIS speech：把它从 `sources_config.py` 里现有的研究文章来源列表中**移除**（或标记
   `feeds_into: "news"` 字段），改为新闻抓取配置的一部分；历史已经入库到 `articles.json`
   的 BIS speech 记录可以保留作为历史存档，不需要回溯删除，只需要从**今天起**新抓到的
   BIS speech 都进新闻库，不再进研究文章库

---

## 四、前端展示要求

参照现有网站风格（`docs/index.html` / `style.css` / `app.js`）：

1. 在顶部导航或页面顶部新增一个 **Tab 切换**："研究文章 / 每周新闻"（两个板块共用同一个
   左侧筛选栏框架，但内容和筛选维度不同）
2. "每周新闻"板块的左侧筛选栏：搜索框 + 按来源筛选（BIS/SEC/ECB/Fed/BoE）+ 按周筛选，
   **不需要**研究文章板块的"研究主题"分类筛选（新闻不套用20多个学术主题标签体系）
3. 新闻卡片展示字段：标题（可点击跳转原文）、发言人 + 日期、中文概括、来源标签、
   "阅读原文 →" 链接。卡片可以比研究文章卡片更紧凑（新闻概括通常比论文摘要短很多）
4. 顶部统计栏增加"本周新闻 N 条"这一统计项

---

## 五、自动化集成

在现有 `.github/workflows/weekly-report.yml` 里增加一步 `scrape_news.py` 的执行，
和现有 `scrape.py` 一样每周一自动跑，抓取结果一并提交推送，网页自动更新。

---

## 六、验收标准

1. 网站可以在"研究文章"与"每周新闻"两个板块间切换
2. 每周新闻板块展示本周新增的新闻，字段包含标题/发言人/日期/中文概括/链接，缺失字段
   （如无法识别发言人）要显示合理的占位文案而不是留空白造成误解
3. BIS speech 不再同时出现在两个板块里
4. 新闻板块的搜索、来源筛选功能可用
5. 完成后需说明：本次新增新闻条数、按来源的条数分布、发言人字段识别成功率、
   以及中文概括是通过LLM生成还是机器翻译兜底
