# 任务：在「法律和金融研究动向」网站新增「学者追踪」板块

## 背景与数据源

数据来自 Excel 文件 `最新版_Top50法学学者与Top50金融学者_无重叠_2026-07-28.xlsx`，包含4个sheet：
- **说明**：榜单概览（法学50人+金融50人，无重叠，围绕"银行—NBFI流动性支持、私人信贷、
  MMF/基金、证券化与影子银行、银行监管、稳定币和数字金融"这一主题构建）
- **Top50法学学者** / **Top50金融学者**：每个sheet从第4行开始是表头，字段为：
  `排名 | 学者 | 主要研究主题 | 主要方法 | 为什么值得追踪 | 相关度 | 研究质量 | 近年活跃度 |
  综合分 | 追踪层级 | 主要来源 | 备注`
  其中"追踪层级"取值为"第一梯队/核心追踪/专题追踪"三档
- **主题索引**：按5个研究问题（银行-NBFI流动性支持、私人信贷与LME、MMF与基金脆弱性、
  证券化与影子银行、稳定币与银行去中介化）交叉索引法学/金融优先作者，可作为学者详情页
  "相关研究问题"的补充展示，非必须

**⚠️ 关键陷阱——"主要来源"列不是统一的"个人主页"字段**：抽查发现该列混合了三种情况：
1. 真正的个人/学校主页（如 `law.columbia.edu/faculty/kathryn-judge`）
2. 具体某一篇论文的链接（如 `nber.org/papers/w32316`——这是论文页不是作者页！金融学者
   sheet里约14/50条是这种情况）
3. 已经是结构化作者页的（如 `nber.org/people/philipp_schnabl`，可直接用）
4. 简历PDF链接（如某学者是 `hbs.edu/.../CV_....pdf`，无法作为可持续追踪的网页来源）

不要假设这一列可以直接当作"个人网站"字段渲染到前端或直接拿去爬——**必须先做一轮解析**，
见下面第一步。

---

## 一、学者主页解析阶段（一次性、需要判断力，不是纯脚本任务）

对100位学者逐一确定其**可持续追踪的信息源**，这一步无法完全脚本化，需要agent执行时
边访问边判断（例如访问一个"主要来源"发现是论文页，就要顺藤摸瓜找到该学者的作者主页/
个人主页）。建议按以下优先级判断每位学者的最终来源类型，写入
`database/scholars_config.json`：

1. **本人个人主页或学校教师主页**（优先级最高，通常有"Publications"/"Working Papers"
   板块，也最能体现"为什么值得追踪"里描述的学术活跃状态）
2. **NBER作者页**（`nber.org/people/{slug}`）——如果"主要来源"给的是某篇NBER论文链接
   （`nber.org/papers/wXXXXX`），访问该论文页面，页面上作者署名通常可点击跳转到其
   NBER作者页，把这个作者页记录为该学者的追踪来源之一。本项目此前已验证NBER的RSS/页面
   结构可靠，抓取模式可直接复用
3. **RePEc/IDEAS作者页**（`ideas.repec.org/f/pxxNNN.html`）——本项目此前验证过RePEc对
   NY Fed/BIS内容的公开镜像完全不设防，同一域名下的经济学者个人作者页（收录该学者在
   RePEc registered author service下的全部工作论文/期刊论文列表）大概率同样可以直接
   访问，值得作为NBER之外的第二条结构化路径，尤其适合覆盖经济学背景更强的学者
4. **SSRN作者主页**——⚠️ 已知不可用：本项目此前测试SSRN站点级Cloudflare人机验证会
   拦截所有自动化请求（包括WebFetch），无法绕过，如果某学者唯一的公开列表在SSRN
   作者页，如实标注"暂无法自动追踪，需人工检查"，不要假装能抓
5. **Google Scholar个人主页**——同样不建议作为自动化抓取目标：反爬严格且有ToS限制，
   仅作为前端展示的"外部链接"之一（如果学者主页上能找到Scholar链接就展示，但不抓取
   其内容）
6. **机构简介页**（如 `bis.org/about/bio....htm` 这类央行官员简介页）——通常只有简介
   没有完整著作列表，可作为"个人信息页"链接展示，但不指望能从这类页面稳定拿到最新论文
   列表，需要另外搭配上面1-3的某一种作为真正的文献追踪源
7. **简历PDF**——不建议作为自动化追踪源（PDF结构解析成本高、更新频率低、格式不统一），
   仅在页面上提供PDF链接供用户自己点开，不纳入自动抓取范围

对每位学者，`scholars_config.json` 中记录：
```json
{
  "name": "Kathryn Judge",
  "category": "法学",
  "rank": 1,
  "tier": "第一梯队",
  "research_topics": "影子银行、银行监管、紧急流动性、监管边界",
  "research_methods": "制度分析；金融中介理论；监管设计",
  "why_track": "将信息缺口、最后贷款人与银行—NBFI风险传导连接起来...",
  "profile_url": "https://www.law.columbia.edu/faculty/kathryn-judge",
  "tracking_sources": [
    {"type": "faculty_page", "url": "https://www.law.columbia.edu/faculty/kathryn-judge"},
    {"type": "nber_author", "url": "https://www.nber.org/people/kathryn_judge"}
  ],
  "tracking_status": "active"  // 或 "manual_only"(暂无法自动追踪,需人工检查)
}
```

**实施建议**：不要试图一次性把100人全部打通。按"追踪层级"分批：先完成"第一梯队"
（法学+金融各约15-20人，具体以Excel中该值实际人数为准）的解析与验证，确认抓取模式
work之后再扩展到"核心追踪""专题追踪"。对确实无法自动化追踪的学者，如实在
`tracking_status` 标注，不要在前端呈现虚假的"最新10篇文献"。

---

## 二、学者详情数据抓取

对每位 `tracking_status == "active"` 的学者，抓取其最新10篇文献（已发表论文或工作论文
均可），字段：**标题、作者、发表日期、摘要**，并将**标题和摘要翻译成中文**（复用本项目
现有的 `deep-translator` 逻辑，`title_zh` / `abstract_zh` 字段命名与现有 `articles.json`
保持一致，方便前端组件复用）。

不同 `tracking_sources.type` 对应不同解析方式：
- `nber_author`：NBER作者页本身是HTML列表，逐条解析论文标题+链接，日期/摘要需要进入
  单篇论文页补全（本项目 `scripts/scrape.py` 里 `enrich_bis_wp` 的写法可直接参考，
  NBER论文详情页同样有 `og:description` 或页面正文摘要）
- `repec_author`：复用本项目已有的 `parse_repec_series` + `enrich_repec_citation`
  （citation_* Highwire元标签）逻辑，只是把"系列列表页"换成"作者列表页"
- `faculty_page`：没有统一模板，需要按学校/学者个案编写解析规则（类似本项目给SAFE、
  Yale JReg写的专用解析器），或者退化为更简单的策略——**只做"变化检测"而不强求完整
  语义解析**：记录该页面上出现的论文标题+链接列表的一份快照，之后每周比较"新增了哪些
  之前没见过的标题/链接"，把新增条目视为候选新文献，再进入该链接抓取标题/日期/摘要。
  这个"diff式"思路比"每次都要理解页面结构"更稳健，适合应对个人主页排版千差万别的现实
- `manual_only`：不抓取，前端展示"该学者暂无法自动追踪最新文献，请通过主页链接自行查看"

---

## 三、前端「学者追踪」板块设计

新增第三个Tab："研究文章 / 每周新闻 / 学者追踪"（与现有Tab并列，交互模式参考本项目
现有实现）。

### 左侧：学者名单

- 按 `category` 分两组："法学学者" "金融学者"，组内按 `rank` 升序排列（不是字母序，
  Excel里的排名就是追踪优先级）
- 每个学者显示：排名 + 姓名，可选：`tier` 标签（第一梯队/核心追踪/专题追踪）用不同
  颜色的小圆点或标签区分
- 支持搜索框按姓名/研究主题过滤

### 右侧：学者详情（点击左侧姓名后展示）

- 顶部：学者姓名 + `profile_url` 链接（"访问个人主页 →"），如果 `tracking_sources`
  里有多个来源（如同时有faculty_page和nber_author）都列出可点击链接
- 研究领域、研究方法、为什么值得追踪——三段文字直接取自Excel对应列，原样中文展示
  （这三列本来就是中文，不需要翻译）
- "最新10篇文献"区块：复用现有研究文章卡片样式（`article-card`），展示标题（英文）+
  中文标题、作者、日期、摘要（英文）+ 中文摘要、原文链接
- 未选择任何学者时，右侧显示提示文案（如"从左侧选择一位学者查看详情"）

---

## 四、每周「学者周报」生成逻辑

与现有 `scripts/scrape.py`（研究文章）、`scripts/scrape_news.py`（新闻）并列，新增
`scripts/scrape_scholars.py`：

1. 对每位 `tracking_status == "active"` 的学者，重新抓取其追踪来源
2. 按论文链接去重，找出"上次抓取后新出现"的条目（与现有增量逻辑一致；`faculty_page`
   类型用"新增标题/链接"的diff式判断，见上文）
3. 汇总成 `database/scholars_updates.json`，字段包含学者姓名、类别、新文献的标题/作者/
   日期/摘要/中文翻译/链接
4. 周报新增章节"## 本周学者动态"，按"法学学者新作""金融学者新作"分组列出，格式与现有
   "各来源新文章"章节一致
5. 如果某学者本周没有新文献，不在周报中出现（不需要每周都把100人列一遍"暂无更新"）
6. 集成进 `.github/workflows/weekly-report.yml`，作为独立一步执行

---

## 五、验收标准与现实预期

1. 左侧学者名单法学/金融分组、按排名排序展示，100人名单与Excel完全一致
2. 点击学者可在右侧看到：主页链接、研究领域、研究方法、追踪理由、最新10篇文献
   （标题/作者/日期/摘要均有中英文）
3. 对暂时无法自动追踪的学者，前端如实提示，不编造文献列表
4. 完成后需要汇报：100人中有多少人成功打通自动追踪（按 nber_author / repec_author /
   faculty_page(diff式) / manual_only 分类统计人数）、抓取失败或需要人工复核的学者名单
5. 每周新增 `scripts/scrape_scholars.py` 的执行不应影响现有研究文章/新闻抓取流程的
   稳定性——三者互相独立，一个失败不应导致另外两个也失败

**现实预期说明**：这是本项目目前技术难度最高的一个板块——此前接入的所有来源
（RSS/JSON:API/RePEc镜像）都是"一个来源对应一份格式统一的feed"，而这次是"100个人，
100种个人主页排版"。建议不要期望首次实现就能100%覆盖全部100人的自动追踪，第一阶段
达成"第一梯队"学者可靠追踪、其余学者诚实标注追踪状态，是合理的验收目标。
